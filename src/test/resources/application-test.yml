spring:
  application:
    name: marginality2

  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
    pool:
      initial-size: 5
      max-size: 20
      max-idle-time: 30m

  docker:
    compose:
      enabled: false

  h2:
    console:
      enabled: true